#ifndef BLUEPADCONTROLLER_H\n#define BLUEPADCONTROLLER_H\n\n#include <stdint.h>\n\n// Struct to represent gamepad input including analog sticks and buttons\nstruct GamepadInput {\n    float leftStickX;   // X axis of the left stick\n    float leftStickY;   // Y axis of the left stick\n    float rightStickX;  // X axis of the right stick\n    float rightStickY;  // Y axis of the right stick\n    bool aButton;       // A button state\n    bool bButton;       // B button state\n    bool xButton;       // X button state\n    bool yButton;       // Y button state\n\n    // Add more buttons as necessary\n};\n\n// Class to manage the BluePad32 input and deadzone management\nclass BluePadController {\npublic: \n    BluePadController();\n\n    // Updates the gamepad input\n    void update();\n\n    // Gets the current gamepad input\n    GamepadInput getInput();\n\n    // Set deadzone for analog sticks\n    void setDeadzone(float deadzone);\n\nprivate:\n    GamepadInput currentInput;\n    float deadzone;\n    void applyDeadzone(float &value);\n};\n\n#endif // BLUEPADCONTROLLER_H\n